"""
 1.1). แบบปกติ

number = int(input("ใส่ตัวเลข: "))
text = ""
for x in range(number):
    text = "*" * (int(x + 1) + x)
    y = " " * (number-(x+1))
    z = y + text
    " " * (number-(x-1)) + "*" * (int(x+1) + x)
    print(z)

----------------------------------------------------------------------------------

 1.2). แบบปกติ (รูปแบบสมการย่อ)

number = int(input("ใส่ตัวเลข: "))
for x in range(number):
    print(" " * (number-(x-1)) + "*" * (int(x+1) + x))


----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
----------------------------------------------------------------------------------

 2.1). แบบวนซ้ำ

number = int(input("ใส่ตัวเลข: "))
for x in range(number):
    text = ""
    for y in range((x + 1) + x):
        text += "*"
        spacebar = " " * int(number-(x+1))
        Finist = spacebar + text
    print(Finist)

----------------------------------------------------------------------------------
 2.2). แบบวนซ้ำ (รูปแบบสมการย่อ)

number = int(input("ใส่ตัวเลข: "))
for x in range(number):
    text = ""
    for y in range((x + 1) + x):
        text += "*"
    print(" " * int(number-(x+1)) + text)

----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
----------------------------------------------------------------------------------
----------------------------------------------------------------------------------

"""